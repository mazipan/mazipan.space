---
layout: '../../layouts/Page.astro'
title: Cards
---
import PostCardSmall from '../../components/PostCardSmall.astro';

import ProjectCard from '../../components/ProjectCard.astro';

import { getAllPostsWithReadingTime } from '../../modules/post/common';
import { getAllProjects } from '../../modules/project';

back to [Index](/design)

export const posts = await getAllPostsWithReadingTime();
export const post = posts[0];
export const threePosts = posts.slice(0, 3);

export const postWithoutImage = {
  ...post,
  data: {
    ...post.data,
    noHero: true,
  },
};

export const projects = await getAllProjects();
export const project = projects[0];
export const threeProjects = projects.slice(0, 3);

---

## PostCardSmall

<div class="not-prose">
  {threePosts.map((post) => (
    <PostCardSmall post={post} withImage={true} />
  ))}
</div>

---

## PostCardSmall Without Image

<div class="not-prose">
  {threePosts.map((post) => (
    <PostCardSmall post={post} withImage={false} />
  ))}
</div>

---

## PostCardSmall With Separator

<div class="not-prose">
  {threePosts.map((post) => (
    <PostCardSmall post={post} withImage={false} withSeparator={true} />
  ))}
</div>

---

## ProjectCard

<div class="not-prose flex gap-4">
  {threeProjects.map((project) => (
    <ProjectCard project={project} />
  ))}
</div>
