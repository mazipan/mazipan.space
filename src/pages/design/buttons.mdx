---
layout: '../../layouts/Page.astro'
title: Buttons, Links, Tags
---
import { Icon } from 'astro-icon/components';

import Button from '../../components/Button.astro';
import Link from '../../components/Link.astro';
import Tag from '../../components/Tag.astro';

back to [Index](/design)

export const buttons = {
  variant: [
    { variant: 'outline', colors: 'primary' },
    { variant: 'solid', colors: 'primary' },
    { variant: 'ghost', colors: 'primary' },
    { variant: 'outline', colors: 'default' },
    { variant: 'solid', colors: 'default' },
    { variant: 'ghost', colors: 'default' },
  ],
  colors: [{ colors: 'primary' }, { colors: 'default' }],
  size: [{ size: 'md' }, { size: 'sm' }],
  disabled: [{ disabled: true }, { disabled: false }],
};

## Buttons

{Object.entries(buttons).map(([key, values]) => (
<>
  <h4>{key}</h4>
  <div class="not-prose flex items-center gap-4 flex-wrap">
    {values.map((props) => {
      const text = `${Object.values(props)[0]}`;
      return <Button {...props} role="button" type="button" element="button">{text}</Button>;
    })}
  </div>
</>
))}

#### slot="before-icon"

<div class="not-prose">
  <Button colors="primary" size="sm" role="button" type="button">
    <Icon name="tabler:device-floppy" slot="before-icon" />
    My Button
  </Button>
</div>

#### slot="after-icon"

<div class="not-prose">
  <Button colors="primary" role="button" type="button">
    <Icon name="tabler:device-floppy" slot="after-icon" />
    My Button
  </Button>
</div>

export const links = {
  variant: [
    { variant: 'link' },
    { variant: 'no-underline' },
    { variant: 'link-heading' },
    { variant: 'link-nav' },
  ],
  disabled: [{ disabled: true }, { disabled: false }],
};

## Links

{Object.entries(links).map(([key, values]) => (
<>
  <h4>{key}</h4>
  <div class="not-prose flex items-center gap-4 flex-wrap">
    {values.map((props) => {
      const text = `${Object.values(props)[0]}`;

      return <Link {...props}>{text}</Link>;
    })}

  </div>
</>
))}

#### link markdown

[/design/buttons](/design/buttons)

export const tags = {
  variant: [
    { variant: 'solid', colors: 'primary' },
    { variant: 'solid', colors: 'default' },
    { variant: 'text', colors: 'primary' },
    { variant: 'text', colors: 'default' },
  ],
  size: [
    { size: 'md', variant: 'solid' },
    { size: 'md', variant: 'text' },
    { size: 'sm', variant: 'solid' },
    { size: 'sm', variant: 'text' },
  ],
};

## Tags

{Object.entries(tags).map(([key, values]) => (
<>
  <h4>{key}</h4>
  <div class="not-prose flex items-center gap-4 flex-wrap">
    {values.map((props) => {
      const item = Object.entries(props)[0];
      const text = `${item[0]} ${item[1]}`;

      return <Tag {...props} role="button" type="button">{text}</Tag>;
    })}

  </div>
</>
))}
