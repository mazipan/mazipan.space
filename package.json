{
  "name": "blog-3.0",
  "description": "Blog 3.0 with Gatsby",
  "version": "0.0.0",
  "private": true,
  "author": "Irfan Maulana <github.com/mazipan>",
  "license": "MIT",
  "scripts": {
    "copy:static": "node ./build-scripts/copy-static.js",
    "copy:talks": "node ./build-scripts/copy-talks.js",
    "lint": "eslint src/**/**/*.tsx",
    "lint:fix": "eslint --fix src/**/*/*.tsx",
    "start": "yarn dev",
    "watch:eleventy": "cross-env NODE_ENV=development eleventy --watch --serve",
    "dev": "npm-run-all --parallel watch:*",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "markdown-it": "^12.0.0",
    "npm-run-all": "^4.1.5"
  },
  "devDependencies": {
    "@11ty/eleventy": "^0.11.0",
    "app-root-dir": "^1.0.2",
    "cross-env": "^7.0.2",
    "rimraf": "3.0.0"
  }
}
